[package]
name = "bind_cpp_macros"
version = "0.1.0"
edition = "2021"

[dependencies]
cpp = "0.5"

[build-dependencies]
cpp_build = "0.5"

[package.metadata]
notice = "Requires C++ compiler with C++17 support"
