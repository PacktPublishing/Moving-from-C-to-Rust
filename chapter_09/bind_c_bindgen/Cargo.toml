[package]
name = "bind_c_bindgen"
version = "0.1.0"
edition = "2021"

[build-dependencies]
bindgen = "0.71"
cmake = "0.1"

[package.metadata]
notice = "Requires libclang to be installed for bindgen to work"
